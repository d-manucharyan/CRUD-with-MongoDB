<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href="/stylesheets/user.css">
    <title>User</title>
</head>

<body>
    <div class="main-container">
        <div class="user-info">
            <h2 style="color: rgb(33, 74, 162);">
                <%=user.firstname%>'s personal information
            </h2>
            <% for(let key in user) { if(key !=='_id' ) { %>
                <li>
                    <%=key%> : <%=user[key] %>
                </li>
                <% } } %>
        </div>

        <div class="edit-form">
            <h3 style="color: rgb(33, 74, 162); font-size: 30px;">Edit user</h3>

            <div class="field">
                <label for="firstname">Firstname</label>
                <input required type="text" id="firstname" value="<%= user.firstname %>">
            </div>

            <div class="field">
                <label for="lastname">Lastname</label>
                <input type="text" id="lastname" value="<%= user.lastname %>">
            </div>

            <div class="field">
                <label for="age">Age</label>
                <input type="number" id="age" value="<%= user.age %>">
            </div>

            <div class="field">
                <label for="email">E-mail</label>
                <input type="email" id="email" value="<%= user.email %>">
            </div>

            <div class="field">
                <label for="address">Address</label>
                <input type="text" id="address" value="<%= user.address %>">
            </div>

            <button id="editBtn" data-id="<%= user._id %>">Edit</button>
        </div>



        <script src="/javascripts/users.js"></script>
</body>

</html>